/**
  * 作用与目的：通过递归创建垂直菜单
  * @item {Oject/Array} 要遍历递归的对象或者数组
  * @padding {Number} 每个垂直菜单的左padding值
  * @return {HTMLElement} 返回的html的菜单节点
  * */
export default function creatVerMenu(item: any, padding?: number): HTMLElement;
